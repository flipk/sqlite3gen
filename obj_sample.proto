
///////////////////////////////////////////////////////////
// NOTE // NOTE // NOTE // NOTE // NOTE // NOTE // NOTE  //
//                                                       //
//       THIS FILE IS AUTOGENERATED BY sqlite3gen        //
// DO NOT EDIT THIS FILE, EDIT THE SOURCE AND REGENERATE //
//                                                       //
// NOTE // NOTE // NOTE // NOTE // NOTE // NOTE // NOTE  //
///////////////////////////////////////////////////////////

syntax = "proto2";

package library;

// NOTE this is PROTOTOP from line 8 of "../sample.schema"

import "sample2.proto";
// proto top line 2



enum TABLE_user_VERSION_t
{
    TABLE_user_VERSION = 19;
}

message TABLE_user_m
{
  /* note this is int32 instead of TABLE_user_VERSION_t
     to prevent protobuf decode errors when version enum
     value changes. we're kinda breaking google's rules a little
     bit by redefining enum values. */
  /*required*/ optional int32 schema_version = 1;
  optional int32 userid = 2;
  optional string firstname = 3;
  optional string lastname = 4;
  optional string mi = 5;
  optional int32 SSN = 6;
  optional double balance = 7;
  optional bytes proto = 8;
  optional bool test2 = 9;
  optional sample.library2.EnumField_t test3 = 10;
  /*SUBTABLE*/ repeated TABLE_checkouts_m checkouts = 11;

}


enum TABLE_book_VERSION_t
{
    TABLE_book_VERSION = 1;
}

message TABLE_book_m
{
  /* note this is int32 instead of TABLE_book_VERSION_t
     to prevent protobuf decode errors when version enum
     value changes. we're kinda breaking google's rules a little
     bit by redefining enum values. */
  /*required*/ optional int32 schema_version = 1;
  optional int32 bookid = 2;
  optional string title = 3;
  optional string isbn = 4;
  optional double price = 5;
  optional int32 quantity = 6;

}


enum TABLE_checkouts_VERSION_t
{
    TABLE_checkouts_VERSION = 1;
}

message TABLE_checkouts_m
{
  /* note this is int32 instead of TABLE_checkouts_VERSION_t
     to prevent protobuf decode errors when version enum
     value changes. we're kinda breaking google's rules a little
     bit by redefining enum values. */
  /*required*/ optional int32 schema_version = 1;
  optional int32 bookid2 = 2;
  optional int32 userid2 = 3;
  optional int64 duedate = 4;

}

// NOTE this is PROTOBOTTOM from line 105 of "../sample.schema"

// proto bottom line 1
// proto bottom line 2
